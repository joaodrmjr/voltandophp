<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ base_url() }}">Reaprendendo</a>
        </div>

        <div id="menu" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>
                    <a href="{{ path_for('home') }}">Home</a>
                </li>

                {% if auth.state == 2 %}
                <li class="dropdown" style="background-color: red;">
                    <a id="adminLabel" href="#" data-toggle="dropdown">
                        Admin
                        <span class="caret"></span>
                        
                    </a>
                    <ul class="dropdown-menu" data-labelledby="userLabel">
                        <li>
                            <a href="#">Teste 1</a>
                        </li>
                        <li>
                            <a href="#">Teste 2</a>
                        </li>
                    </ul>
                </li>
                {% endif %}
            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if auth.state %}
                <li>
                    <a href="#">R$ 0,00</a>
                </li>
                <li class="dropdown">
                    <a id="userLabel" href="#" data-toggle="dropdown">
                        {{ auth.user.username }}
                        <span class="caret"></span>
                        
                    </a>
                    <ul class="dropdown-menu" data-labelledby="userLabel">
                        <li>
                            <a href="{{ path_for('changepw') }}">Alterar Senha</a>
                        </li>
                        <li>
                            <a href="{{ path_for('logout') }}">Logout</a>
                        </li>
                    </ul>
                </li>
                {% else %}
                <li>
                    <a href="{{ path_for('login') }}">Login</a>
                </li>
                <li>
                    <a href="{{ path_for('register') }}">+ Cadastrar</a>
                </li>
                {% endif %}
            </ul>
        </div>
	</div>
</nav>