<div class="col">
	<span>{{ item.name }} {{ item.last_name }}</span>
	<a href="#" data-action="{{ path_for('ajax.delete', { 'id': item.id }) }}" data-id="{{ item.id }}" class="btn btn-danger">Deletar</a>
</div>